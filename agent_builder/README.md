# 🛠️ Сборщик Агента ITBase (Windows)

Этот каталог содержит исходный код и инструменты для компиляции `exe`-файла агента инвентаризации.

> ⚠️ **КРИТИЧЕСКИ ВАЖНО:** Сборка агента возможна **ТОЛЬКО в среде Windows**.
> Не пытайтесь запускать `build.bat` внутри Docker-контейнера или на Linux-сервере — это не сработает, так как библиотеки `wmi` и `pyinstaller` требуют ядра Windows.

## 🔄 Сценарий сборки (Workflow)

Поскольку основной проект ITBase развернут на Linux/Docker, процедура выглядит так:

1.  **Экспорт:** Скопируйте эту папку (`agent_builder`) из репозитория на любой компьютер с **Windows** (ваш рабочий ноутбук).
2.  **Подготовка:** Убедитесь, что на Windows установлен **Python 3.10+** (при установке обязательно поставьте галочку **"Add Python to PATH"**).
3.  **Сборка:** Запустите файл `build.bat` двойным кликом.
4.  **Результат:** Скрипт сам скачает зависимости, скомпилирует код и положит готовый `ITBase_Agent.exe` в эту же папку.

## 🚀 Использование Агента

1.  Передайте файл `ITBase_Agent.exe` пользователям (через Telegram, почту или общую папку).
2.  Пользователь запускает файл.
3.  Скрипт сканирует оборудование и создает рядом файл `inventory_<ИМЯ_ПК>.json`.
4.  Этот JSON-файл загружается в веб-интерфейс ITBase (вкладка "Компоненты" в карточке актива).

## 📂 Структура

*   `src/agent.py`: Исходный код логики сбора данных (WMI, Registry).
*   `src/requirements.txt`: Зависимости только для агента (не добавлять в основной проект!).
*   `build.bat`: Скрипт-автоматизатор ("One-click build").